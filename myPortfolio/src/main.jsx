import React from "react";
import ReactDOM from 'react-router-dom';
import App from './App.jsx';

ReactDOM.createRoutesFromElements(document.getElementById('root').render (
    <React.StrictMode>
        <App/>
        </React.StrictMode>,
))